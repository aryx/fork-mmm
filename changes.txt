# -*- org -*-

* 0.42 (Q1 2026) (resume with 4.14.2, dune, Dockerfile/GHA, semgrep, caps)

** build infra
- resume compilation of mmm with OCaml 4.14, fixed many unused
  vars warnings, many String -> Byte, remove use of 
  camlp4 stream extension (removed from 4.08), and a few more fixes
- build also with dune to get better warnings and code navigation
  in efuns and use OPAM for some dependencies

** test infra
- Dockerfile and docker.yml Github Actions CI check
- semgrep.jsonnet and semgrep.yml CI check

** internals
- introduce intermediate libs/, languages/, src/, and gui/ folders
  (like in my other projects such as codemap/efuns/osemgrep)
- copy commons/ from xix and start to use capabilities!

* beta (Q1 2015) (maintenance)

* alpha (Q1 2011) (fork)

** build infra 
- compile with OCaml 3.12

** internals
- reorganize folders and make (OCaml) applets code optional

* -----------------------------------------------------------------------

* 0.418

* 0.417

* Pre 0.417
